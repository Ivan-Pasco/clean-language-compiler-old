start()
	string text = "Hello"
	integer len = StringUtils.length(text)
	print("Length: " + len.toString())
	
	string world = " World!"
	string combined = StringUtils.concat(text, world)
	print("Combined: " + combined) 