start:
	string text = "Hello"
	integer len = string.length(text)
	print("Length: " + len.toString())

	string world = " World!"
	string combined = string.concat(text, world)
	print("Combined: " + combined)