start()
	// Test combined math and string namespace functionality

	// Math namespace tests
	number pi_value = math.pi()
	number sqrt_result = math.sqrt(16.0)
	number trunc_result = math.trunc(3.7)
	print("Math namespace tests complete")

	// String namespace tests
	string text = "Hello World"
	number text_length = string.length(text)
	boolean has_world = string.contains(text, "World")
	print("String namespace tests complete")

	// Matrix operations (from previous fixes)
	matrix<number> numbers = [[1.0, 2.0], [3.0, 4.0]]
	print("Matrix operations test complete")

	print("All namespace and core features working!")