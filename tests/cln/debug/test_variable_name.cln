class Parent
	string name

	constructor(string parentName)
		name = parentName

class Child is Parent
	boolean isIndoor

	constructor(string childName, boolean indoorFlag)
		base(childName)
		isIndoor = indoorFlag

start()
	Child test = Child("test", true)
	print("done")