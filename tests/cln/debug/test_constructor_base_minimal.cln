class Base
	string name

	constructor(string baseName)
		name = baseName

class Child is Base
	constructor(string childName)
		base(childName)

	functions:
		string first()
			return "first"

		string second()
			return "second"