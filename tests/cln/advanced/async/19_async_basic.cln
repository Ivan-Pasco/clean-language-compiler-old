
functions:
	string fetchData(string url)
		return "Data from " + url

	string processData(string data)
		return "Processed: " + data

start()
	string data = fetchData("api.example.com")
	string processed = processData(data)
	print(processed)