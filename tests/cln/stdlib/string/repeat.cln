// Test: stdlib/string/repeat
// Grammar: grammar.ebnf method_call — string.repeat(count)
// Semantic: string.repeat(integer) -> string; repeats the string n times
// Expected output:
//   repeat ok

start:
    string s = "ab"
    string r = s.repeat(3)
    print("repeat ok")
