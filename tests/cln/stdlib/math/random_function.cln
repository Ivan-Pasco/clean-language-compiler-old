// Test: stdlib/math/random_function
// Grammar: grammar.ebnf function_call — math.random() is a zero-argument function
// Semantic: math.random() -> number; returns a value in [0.0, 1.0)
// Expected output:
//   random ok

start:
    number r = math.random()
    // We cannot assert the exact value, but we can check it compiles and runs
    // Verify it returns a number type by storing and using it
    number doubled = r + r
    print("random ok")
