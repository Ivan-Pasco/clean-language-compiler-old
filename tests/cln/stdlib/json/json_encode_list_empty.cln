// Test: stdlib/json/json_encode_list_empty
// Semantic: FUNC-BUILTIN — json.encode on an empty list short-circuits to "[]"
//   inside the typed-list helper.
//
// Expected output:
//   []

start:
	list<string> empty = []
	print(json.encode(empty))
