// Test: stdlib/json/json_encode_list_integer
// Semantic: FUNC-BUILTIN — json.encode dispatches to __json_encode_cln_list
//   with elem_tag = 1 (Integer); each element is converted via int_to_string.
// Regression: see json_encode_typed_list.cln header.
//
// Expected output:
//   [1,2,3]

start:
	list<integer> ns = [1, 2, 3]
	print(json.encode(ns))
