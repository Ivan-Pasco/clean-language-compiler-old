// Test: JSON module - null only
// Minimal test to isolate the issue

functions:
	void testParseNull()
		integer data = json.textToData("null")
		print("Parsed null") +

start()
	testParseNull()
	print("Test completed") +
