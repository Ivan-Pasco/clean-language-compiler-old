start:
	print("Endpoints DSL test completed")
functions:
	list<string> listUsers()
		return ["Alice", "Bob", "Charlie"]

	string getUser(string id)
		return "User " + id

	string createUser()
		return "User created"

// Spec Section: 18 - Plugin System
// Tests: endpoints: DSL block

endpoints:
	GET "/users" -> listUsers
	GET "/users/{id}" -> getUser
	POST "/users" -> createUser

