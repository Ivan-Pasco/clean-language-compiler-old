start:
	string result1 = processInt(42)
	string result2 = processStr("test")

	print(result1)
	print(result2)// Spec Section: 6.3 - Function Declaration
// Spec Section: 6.4 - Parameters (different parameter types)
// Tests: Functions with same name but different parameter types (simulated overloading)

functions:
	string processInt(integer x)
		return "Integer processed"

	string processStr(string x)
		return "String processed"

