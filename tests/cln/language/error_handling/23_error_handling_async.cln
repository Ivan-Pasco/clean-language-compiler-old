
functions:
	string processDataAsync(string data)
		if data == "bad"
			return "Processing error"
		return "Processed: " + data

start:
	string result1 = processDataAsync("test")
	print(result1)

	string result2 = processDataAsync("bad")
	print(result2)