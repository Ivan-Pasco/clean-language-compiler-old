// Test framework functionality

functions:
	integer add(integer a, integer b)
		return a + b

	boolean isPositive(integer value)
		return value > 0

tests:
	"add function works correctly": add(2, 3) = 5
	"positive number test": isPositive(10) = true
	isPositive(0) = false

start()
	// Nothing needed here for tests