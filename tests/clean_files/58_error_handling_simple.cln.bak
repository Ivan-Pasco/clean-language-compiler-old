// Error handling simple test - demonstrates basic error patterns
// Note: Full onError syntax requires parser implementation

functions:
	integer safeDivide(integer a, integer b)
		if b == 0
			return 0  // Simple fallback instead of error()
		return a / b
	
	string readFileContents(string filename)
		if filename == ""
			return "default content"  // Simple fallback
		return "file contents"
	
	void testErrorHandling()
		// Test safe division with fallback
		integer result1 = safeDivide(10, 2)
		print "Safe division result: " + result1.toString( ln)
		
		// Test division by zero handling
		integer result2 = safeDivide(10, 0)
		print "Division by zero handled: " + result2.toString( ln)
		
		// Test file reading with fallback
		string contents1 = readFileContents("valid.txt")
		print "File contents: " + contents1 ln
		
		// Test empty filename handling
		string contents2 = readFileContents("")
		print "Error handled: " + contents2 ln

start()
	testErrorHandling()
	print "Error handling test completed successfully" ln