class Base
	string name
	
	constructor(string baseName)
		name = baseName

class Child is Base
	boolean flag
	
	constructor(string childName, boolean childFlag)
		base(childName)
		flag = childFlag

start()
	Child child = Child("test", true)
	print(child.name)